# Postgres (Neon via Vercel). Sensitive on Vercel — `vercel env pull` leaves them empty.
# For local cloud backup, paste from Neon/Vercel dashboard. The app works without them (localStorage).
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
DATABASE_URL=
DATABASE_URL_UNPOOLED=
